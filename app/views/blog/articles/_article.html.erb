<article>
  <header>
    <h1 class='title'>
      <%= link_to article.title, article_path(article) %>
    </h1>
  </header>
  <footer>
    <%= localize(article.created_at.to_date, format: :long) %>
  </footer>
  <div>
    <%= render_text(article.body) %>
  </div>
</article>
